{
  "name": "aftermeet",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev public --kv EVENTS --kv PROFILES --port 8788",
    "deploy": "wrangler pages deploy public --project-name aftermeet"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
